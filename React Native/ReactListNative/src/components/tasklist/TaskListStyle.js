import { StyleSheet } from "react-native";


export const TaskListStyle = StyleSheet.create({
    taskListContainer: {
        flex: 1,
    },

    viewBox: {
        padding: 10,
        borderColor: "red"
    }
})